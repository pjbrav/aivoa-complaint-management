# Base URL for the FastAPI backend. In dev, Vite proxies /api to this
# automatically (see vite.config.js), so this is mainly for production
# builds where the frontend is served separately from the backend.
VITE_API_BASE_URL=/api
